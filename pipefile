#!groovy

properties([disableConcurrentBuilds()])

pipeline {
   agent any
   
   options {
   timestamps()
   }
   stages ('First stage') {
         steps {
              echo 'Building...'
              sh 'ssh root@192.168.1.23 \'hostname\''
        }
    }

}
